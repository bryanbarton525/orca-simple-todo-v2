import React, { useState } from 'react';\n\nconst SettingsForm = ({ onSave }) => {\n  const [url, setUrl] = useState('');\n  return (\n    <form\n      onSubmit={e => {\n        e.preventDefault();\n        onSave(url);\n      }}\n      aria-label="Settings form"\n    >\n      <label htmlFor="feedUrl" className="block text-sm font-medium text-gray-700">\n        Feed URL\n      </label>\n      <input\n        id="feedUrl"\n        type="url"\n        value={url}\n        onChange={e => setUrl(e.target.value)}\n        className="mt-1 block w-full border border-gray-300 rounded-md shadow-sm p-2"\n        required\n      />\n      <button\n        type="submit"\n        className="mt-4 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700"\n      >\n        Save\n      </button>\n    </form>\n  );\n};\n\nexport default SettingsForm;\n