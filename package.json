{
  "name": "todo-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node server.js",
    "build": "echo \"Build step placeholder\""
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
