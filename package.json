{
  "name": "todo-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}