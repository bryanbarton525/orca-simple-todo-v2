export const feeds: string[] = [
  // Add your RSS feed URLs here
  "https://news.google.com/rss",
  "https://rss.nytimes.com/services/xml/rss/nyt/HomePage.xml",
];

// To add a new feed, simply append its URL to the array.
// To remove a feed, delete the corresponding line.

// Example:
// feeds.push("https://example.com/feed.xml");
// feeds.splice(0, 1); // remove first feed
